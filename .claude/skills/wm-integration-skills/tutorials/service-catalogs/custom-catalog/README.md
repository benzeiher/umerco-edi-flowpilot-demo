# Custom Service Catalog

This directory contains JSON service contract files for project-specific or domain-specific services that extend the built-in webMethods catalog.

To make a custom service available to AI skills during FSL generation, add a JSON file that follows the schema in `wm-service-catalog/`. 
